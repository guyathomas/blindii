import type { WidgetConfig, ProductDetailsWidgetProps } from "@medusajs/admin";

export { default } from "./product-details-after";

export const config: WidgetConfig = {
  zone: "product.details.after",
};
